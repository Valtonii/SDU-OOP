/*

• In which order is what calculated?
its <, && and then =

• Which values (named and otherwise) are each operator evaluated on? • What are the types of these values?
the result of the left side is true, since price is less than (<) the budget. both are doubles.

the values at the AND (&&) are evaluated on the comparison result and requiredreading being true. both are bools at this point and its true.

now for the last part, =. shouldBuy and the final result true, both are bools.


• What does the variable shouldBuy represent?
The variable shouldBuy represents a boolean flag (a true/false decision) that determines whether a specific book or item should be purchased.

*/
